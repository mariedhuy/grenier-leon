<div class="container">
  <h1>Mes objets</h1>
  <div class="cards">
    <% @items.each do |item| %>
      <div class="card-request">
        <%= image_tag(item.picture) %>
        <div class="card-request-infos">
          <h2><%= item.name %></h2>
          <p><%= item.category %></p>
          <p>Objet appartenant à <%= item.user.name %></p>
        </div>
    </div>
      <% end %>
  </div>
</div>
